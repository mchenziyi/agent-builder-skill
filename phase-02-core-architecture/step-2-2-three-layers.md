# Step 2.2 — 区分三层职责

| 层 | 做 | 不做 |
|---|---|---|
| Workflow | 定义流程、条件分支 | 不推理决策 |
| Agent | 分析、选工具、回复 | 不直接执行 |
| Tools | 调 API、查 DB、操作文件 | 不做判断 |

**边界判断：** 需要 LLM 判断就是 Agent 的活，条件分支就是 Workflow 的活。
